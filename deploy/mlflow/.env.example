# Postgres — pick a strong password, then paste it into the URI below
POSTGRES_DB=mlflow
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# MLflow — replace CHANGE_ME_TO_A_STRONG_PASSWORD with the same password above
MLFLOW_BACKEND_STORE_URI=postgresql://mlflow:CHANGE_ME_TO_A_STRONG_PASSWORD@postgres:5432/mlflow
MLFLOW_ARTIFACT_ROOT=s3://sanskrit-shloka-bucket/mlflow

# AWS credentials for S3 artifact storage
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
